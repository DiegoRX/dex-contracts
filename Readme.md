git clone

install node js LTS: 
https://nodejs.org/en/

install truffle:
npm i -g truffle

install dependencies:
npm install

Deploy to testnet
truffle migrate --network mumbai --reset

Deploy to Mainnet
truffle migrate --network polygon --reset